# 회의실 예약 대시보드 — 인수인계서

> 작성일: 2026-06-16
> 인계자: 박석주 (seokjoo@dcamp.kr)
> 대상: 디캠프 회의실 예약 운영자

---

## 1. 이게 뭐 하는 도구인가

투자전략팀이 **타 팀 회의를 위해 회의실을 일괄로 잡아주는 일**을 자동화한 Streamlit 웹앱입니다. 운영자는 매주 회의 일정을 모아 회의실을 배정하고, 그 결과를 안내 문자로 정리해 카톡/문자로 공유합니다.

**핵심 아이디어 — 미러 이벤트(mirror event)**
- 회의 원본은 각 팀 주최자가 만듭니다. 운영자는 남의 일정을 수정할 권한이 없습니다.
- 운영자가 "회의실을 잡는다"는 것은 → **운영자 본인 캘린더에 같은 시간대 이벤트를 만들고, 회의실 리소스만 초대**하는 것입니다. 이게 "미러 이벤트".
- 미러 이벤트에는 원본 회의의 ID가 심어져 있어, 나중에 다시 매칭이 가능합니다.

---

## 2. 어디서 쓰는가

| 항목 | 값 |
|---|---|
| 운영 URL | https://dcampcalerderslave-ppqgkxai9nddumkpn6dzjg.streamlit.app/ |
| GitHub 레포 | https://github.com/contangolife/DCAMP_Calender_Slave |
| 배포 플랫폼 | Streamlit Cloud (GitHub 연동, main 푸시 시 자동 배포) |
| Google Cloud 프로젝트 | OAuth 2.0 Client ID — Streamlit Secrets에 보관 |

---

## 3. 일상 사용법 (운영자용)

### 3-1. 로그인
1. 운영 URL 접속 → "Google 계정으로 로그인" 클릭
2. **반드시 본인의 @dcamp.kr 계정**으로 로그인 (회의실 권한이 그 계정에 묶여 있음)
3. 로그인 후 새 탭에서 대시보드가 열림 — 원래 탭은 닫아도 됨

### 3-2. 화면 구성
- **상단**: 주차 선택 (이번주 / 다음주 / 다다음주) + 새로고침 버튼
- **요일 탭**: 월~금 + "📋 문자 생성"
- **각 요일 화면**:
  - 상단 가로 스트립: 팀원별 그 날의 휴가/근무지/종일 일정 (회의실 예약과 무관)
  - 회의 목록 테이블: 팀 / 소유자 / 회의 제목 / 시간 / 참석자 / 회의실 현황 / 회의실 선택

### 3-3. 회의실 예약하기
1. 회의실 현황이 **🔴 미배정**인 행 찾기
2. 맨 오른쪽 "회의실 선택" 드롭다운에서 원하는 방 선택 (여러 개 선택 가능 = 동일 회의에 회의실 2개 잡기)
3. 다른 회의에도 같은 방식으로 선택
4. 상단의 **🚀 일괄 예약 (N건)** 버튼 클릭 → 한 번에 처리
5. 결과 expander에서 성공/실패 확인

**드롭다운에서 보이지 않는 회의실 = 그 시간대에 이미 다른 곳에서 예약된 방.**
같은 배치 내 다른 회의에서 이미 선택한 방도 시간 겹치면 자동으로 빠집니다.

### 3-4. 예약 취소
1. 회의실 현황 열에 **✅ 회의실명 ▾** 버튼이 보이면 (= 내가 예약한 거)
2. 클릭 → popover 열림 → 각 회의실 옆의 **✕** 버튼 클릭 → 취소됨

### 3-5. 시간 동기화 (🔁)
- 회의 원본의 시간이 변경되었지만 내가 잡아둔 회의실은 옛 시간 그대로일 때 발생
- 트리거 라벨이 **⚠️**로 바뀌고, popover 안에 **🔁** 버튼이 나타남
- 클릭 한 번이면 옛 예약 취소 + 새 시간으로 같은 방 재예약을 자동으로 처리

### 3-6. 빈 예약 (= 원본 회의가 사라진 예약) 정리
- 화면 상단에 **⚠️ 원본 회의가 없는 예약 N건** 경고가 뜰 수 있음
- = 내가 회의실을 잡아뒀는데 원본 회의가 삭제되었거나 다른 주차로 옮겨진 경우
- 회의실은 계속 점유 중(다른 사람이 못 잡음)이므로 **✕ 취소** 눌러서 풀어줘야 함

### 3-7. 안내 문자 생성
1. "📋 문자 생성" 탭 클릭
2. 일자별로 정리된 안내 문구가 박스로 표시됨
3. 박스 내용 복사 → 단톡방 등에 붙여넣기

---

## 4. 코드 구조 (개발자용)

```
구글 드라이브 프로젝트/
├── dashboard.py          # Streamlit UI 메인 — 화면 렌더링 + 인증 흐름
├── calendar_api.py       # Google Calendar/People API 호출 로직
├── config.py             # 팀원/회의실/타임존 설정
├── requirements.txt      # 의존성
├── credentials.json      # 로컬 개발용 OAuth Client (배포 시 secrets로 대체)
└── token.json            # 로컬 개발용 토큰 캐시 (배포 시 미사용)
```

### 4-1. dashboard.py 주요 흐름
1. **인증** — Streamlit Secrets가 있으면 per-user OAuth, 없으면 로컬 token.json
2. **주차 선택 + 캐시 로드** — `fetch_team_events`, `fetch_my_bookings`, `fetch_room_busy_times`를 일괄 호출 후 session_state에 보관
3. **헬퍼**
   - `_cancel_mirror(orig_id, mid, mroom, span)` — 미러 1건 취소 + 캐시 in-place 패치
   - `_mirror_time_info(mirror, ev_start, ev_end)` — 원본/미러 시간 비교
   - `_safe_esc(text)` — HTML+마크다운 이스케이프
   - `render_team_strip(date_str)` — 팀 현황 가로 스트립
4. **고아 예약 섹션** — 메인 테이블 위에 별도로 노출
5. **요일별 테이블** — 회의실 선택 + 일괄 예약 처리

### 4-2. calendar_api.py 주요 함수
| 함수 | 역할 |
|---|---|
| `authenticate()` | 로컬 fallback 인증 (token.json) |
| `has_oauth_config()` / `get_auth_url()` / `exchange_code_for_credentials()` | Streamlit Cloud용 per-user OAuth 흐름 |
| `build_service_from_creds_json()` | 토큰 → service 객체 (자동 갱신 포함) |
| `fetch_team_events()` | TEAM_MAP 팀원들의 한 주치 일정 일괄 조회 |
| `fetch_my_bookings()` | 내가 만든 미러 이벤트 조회 (BOOKING_TAG로 필터) |
| `fetch_room_busy_times()` | freebusy API로 회의실 점유 시간 일괄 조회 |
| `book_meeting()` | 미러 이벤트 생성 (사전 중복/충돌 체크 포함) |
| `cancel_booking()` | 미러 이벤트 삭제 (mirror_event_ids로 특정 1건만 가능) |
| `build_email_to_name()` | People API로 이메일 → 한국어 이름 매핑 |
| `build_sms()` | 안내 문자 텍스트 생성 |

### 4-3. config.py에서 손볼 곳
- **`TEAM_MAP`** — 팀원 입사/퇴사/이동 시 추가/삭제. `이메일: 팀명` 형식.
- **`TEAM_ORDER`** — 화면 표시 순서.
- **`ROOM_RESOURCES`** — 회의실 추가/제거. 리소스 ID는 Google Admin Console > 빌딩 및 리소스에서 확인.
- **`SCOPES`** — Calendar + Contacts 읽기. 변경 시 OAuth 재동의 필요.

---

## 5. 운영자 인수인계 체크리스트

### 5-1. 신규 운영자가 같은 dcamp 내부인 경우
- [ ] 신규 운영자 이메일을 Google Cloud Console > OAuth 동의 화면 > 테스트 사용자에 추가
- [ ] `config.py`의 `TEAM_MAP`에 신규 운영자 이메일이 들어가 있는지 확인 (자기 자신이 표시되는 게 자연스러우면 추가)
- [ ] 신규 운영자가 운영 URL에서 본인 Google 계정으로 로그인 가능한지 확인
- [ ] 이전 운영자(나)의 미러 예약이 남아 있으면 일괄 취소 후 새 운영자가 다시 예약 (이전 운영자 캘린더에 박혀있어서 본인 외에는 취소 불가)

### 5-2. Streamlit Cloud 접근권한
- [ ] https://share.streamlit.io 접속 → 신규 운영자를 워크스페이스에 초대
- [ ] Secrets에 등록된 `google_credentials` / `google_token`은 그대로 두면 됨 (per-user OAuth라 사용자별로 따로 관리됨)

### 5-3. GitHub
- [ ] `contangolife/DCAMP_Calender_Slave` 레포에 신규 운영자 collaborator 추가
- [ ] main 푸시 시 Streamlit Cloud가 자동 재배포함을 인지

---

## 6. 자주 발생하는 문제

| 증상 | 원인 / 해결 |
|---|---|
| 로그인 실패 ("로그인 실패: ...") | Google Cloud Console > OAuth 동의 화면 > 테스트 사용자에 본인 이메일이 없음. 추가 후 재시도. |
| 일정이 안 보임 | 1) 주차 선택을 다시 해보거나 "새로고침" 클릭. 2) 다른 팀원이 일정을 비공개로 설정했을 가능성. |
| 회의실 예약은 됐는데 안내 문자에 안 나옴 | 캐시 미갱신. "새로고침" 클릭. |
| 회의실 예약 후 회의 주최자가 시간 변경 | 회의실 현황 ⚠️ → popover의 🔁 클릭으로 동기화. |
| 회의실 잘못 잡음 | popover의 ✕ → 다시 미배정 상태됨 → 다른 방 선택. |
| 회의가 사라졌는데 회의실은 잡혀 있음 | 상단 "원본 회의가 없는 예약" 경고에서 ✕ 클릭. |
| 회의실 드롭다운에 옵션이 적음 | "예약중 N개 숨김"이 표시됨 = 그 시간대에 다른 곳에서 잡힌 회의실은 제외됨. 정상 동작. |
| 배포가 안 됨 | Streamlit Cloud Logs 확인. requirements.txt 문법 오류, secrets 누락이 흔한 원인. |

---

## 7. 외부 조직으로 이전할 때 (참고)

같은 dcamp 내부 인계가 아니라 완전 외부 조직으로 가져가는 경우는 다음을 추가로 작업해야 합니다:

1. **새 Google Cloud 프로젝트** — OAuth 2.0 Client ID 신규 발급
2. **`dashboard.py:48` REDIRECT_URI** 하드코딩 — 자기 배포 URL로 교체
3. **`config.py`의 TEAM_MAP / ROOM_RESOURCES** — 자기 조직 값으로 전부 교체
4. **회의실 리소스 ID** — Google Admin Console에서 자기 조직 회의실 ID 조회
5. **Streamlit Cloud 재배포** — 새 레포 + 새 secrets

> 이 부분은 별도로 코드 휴대성 개선 작업(env 변수화, README 작성) 후 진행하는 게 깔끔합니다.

---

## 8. 향후 개선 후보 (작성자 메모)

`requirements.txt`에 메모 형태로 남겨뒀던 TODO:

- 회의실 예약 변경 (기존 예약을 다른 방으로 한 번에 옮기는 기능)
- 동일 회의에 회의실 2개 잡는 시나리오 정식 지원 (현재는 multiselect로 가능하긴 함)
- 임의 이메일 검색해서 그 사람 일정 조회 기능
- 안내 문자 포맷 커스터마이징

이 TODO들은 `requirements.txt`에서 분리해서 별도 `NOTES.md`로 옮기는 게 좋습니다.

---

**문의:** 박석주 (seokjoo@dcamp.kr)
